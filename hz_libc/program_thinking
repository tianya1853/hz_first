@ 使用结构体即类或对象作为参数传递，具有极强的扩展性，不需要修改接口只需要增加对象内的属性即可，有必要寻找一个编程语言代替c了，但在嵌入式里不太容易实现，除了糟糕的c++

@ 或许可以使用对象来代替所有的基本数据类型

@ 参考几个c中的面向对象编程
@ 所有参数传递不使用基本变量全部使用对象变量，比如一个全局变量，可以直接定义int类型，但是当再需要一个全局变量时，必须再定义一个变量出来，但使用对象则只需要扩展原有的对象属性即可，其他所有的变量传递等等都不需要动